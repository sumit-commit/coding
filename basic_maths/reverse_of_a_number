/*
TC - O(n)
SC - O(1)



*/
int reverse(int n)
{
    int rev = 0, lastdigit = 0;
    while(n > 0)
    {
        lastdigit = n%10;
        rev = rev*10 + lastdigit;
        n = int(n/10);
    }
    return rev;
}
