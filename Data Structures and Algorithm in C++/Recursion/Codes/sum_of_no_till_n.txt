Tail Recursion

#include <iostream>
using namespace std;


int sum(int n)
{ static int ans = 0;
    if(n == 0)
    { 
        return ans ;
    }
   ans += n;
   return sum(n-1);
}

int main()
{
    int x;
    cin>>x;
    cout<<"Sum till "<<x<<"is : "<<sum(x);
    return 0;
}
