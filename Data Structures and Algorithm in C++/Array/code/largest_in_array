//Brute-force

int largest(vector<int> arr)
{
  sort(arr.begin(), arr.end());
  return arr[arr.size()-1];
}


//Optimal

int largest(vector<int> arr)
{
  int max = 0;
  for(int i=0 ; i<arr.size() ; i++)
  {
      if(arr[i] > max)
      {
          max = arr[i];
      }
  }
  return max;
}


