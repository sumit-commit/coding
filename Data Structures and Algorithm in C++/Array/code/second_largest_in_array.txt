//Brute-force
//TC - O(nlogn) + O(n) = O(n)
//SC - O(1)

int slargest(vector<int> arr){

  sort(arr.begin(),arr.end());
  for(int i= arr.size()-2 ; i>0 ; i--)
  {
      if(arr[i] != arr[i-1])
      {
        return arr[i-1];
      }
  }
  return -1;
}
